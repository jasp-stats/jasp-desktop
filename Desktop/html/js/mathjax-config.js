MathJax = {
    options: {
      menuOptions: {
        settings: {
          assistiveMml: false,   // Disable assistive MathML to avoid duplicated copy-past in notes.
        }
      }
    }
  };
  