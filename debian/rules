#!/usr/bin/make -f
#DH_VERBOSE = 1

DPKG_EXPORT_BUILDFLAGS = 1
include /usr/share/dpkg/default.mk

%:
	dh $@ --buildsystem=qmake --parallel

override_dh_auto_configure:
	dh_auto_configure -- CONFIG+=with_dependencies JASP.pro
