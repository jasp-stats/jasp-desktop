example2_run <- function(dataset=NULL, options, perform="run", callback=function(...) 0, state=NULL, ...)
{
	stop('example2_run is not actually implemented but you know, it starts!')
}

example3_run <- function(dataset=NULL, options, perform="run", callback=function(...) 0, state=NULL, ...)
{
	stop('example3_run is not actually implemented but you know, it starts!')
}

example4_run <- function(dataset=NULL, options, perform="run", callback=function(...) 0, state=NULL, ...)
{
	stop('example4_run is not actually implemented but you know, it starts!')
}